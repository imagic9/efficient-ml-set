"""The M1-M4 optimization ladder: PTQ, QAT and structured pruning.

Each candidate starts from the artifact named in DESIGN §8, not from the previous
rung, so the comparison is not a misleading sequential chain.
"""
